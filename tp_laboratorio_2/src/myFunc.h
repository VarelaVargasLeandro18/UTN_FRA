/* \brief ask for a variable of int type.
 * \param char* msg message that will be shown to the user.
 * \return int value that the user inputs.
 */
int get_int (char* msg);

/* \brief ask for a variable of float type.
 * \param char* msg message that will be shown to the user.
 * \return float value that the user inputs.
 */
float get_float (char* msg);
